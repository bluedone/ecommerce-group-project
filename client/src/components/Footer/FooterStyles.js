import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  column: {
    position: 'relative',
    top: '15vh',
    textAlign: 'left',
    color: '#FDFAF6',
  },
}));

export default useStyles;
